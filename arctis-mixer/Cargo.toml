[dependencies]
pipewire = "0.8"       # Idiomatic Rust bindings for PipeWire
hidapi = "2.6"         # Low-level HID access for the dial
tokio = { version = "1", features = ["full"] } # For async management
serde = { version = "1.0", features = ["derive"] } # For config files